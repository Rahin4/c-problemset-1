//Implement a program to find the largest and smallest element in an array.
/*
#include <stdio.h>

void maxmin_finder(int arr[], int n){
    int i , max=arr[0], min=arr[0];
    for (i=0;i<n;i++){
        if(arr[i]>max){
            max=arr[i];
        }
        if(arr[i]<min){
            min=arr[i];
        }
    }
    printf("\n--- Result ---\n");
    printf("%d is max in that array \n",max);
    printf("%d is min in that array\n ",min);

}

int main(){
    int len;
     printf("enter length of the array : ");
     scanf("%d",&len);
     int arr[len];

    if(len <= 0){
        printf("Array size must be positive!\n");
        return 1;
    }

    printf("Enter element of %d: ", len);
    for(int i=0;i<len;i++){
        scanf("%d",&arr[i]);
    }

    maxmin_finder(arr, len);
} */




