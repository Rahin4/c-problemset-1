//Check if a number is prime or not.
/*
#include <stdio.h>
#include <math.h>

int main(){
    int n, i, isprime=1;
    printf("enter a number to check for primeability : ");
    scanf("%d",&n);
        if (n <= 1) {
        printf("%d is not a prime number.\n", n);
        return 0;
    }
    for(i=1;i<sqrt(n);i++){
        if(n%2==0){
            isprime=0;
            break;
        }
    }
    if(isprime){
        printf("%d is prime",n);

    }else{
        printf("%d is not prime", n);
    }
}
*/
